import React from "react";
import Button from "../UI/Button";

const HeroSection = () => {
    return (
        <div>
            <Button variant="secondary" text="Checking" />
        </div>
    );
};

export default HeroSection;
